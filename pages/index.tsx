import type { NextPage } from 'next';
import { useRouter } from 'next/router';
import Container from 'containers/UsersPage';

const Home: NextPage = () => {
  return <Container />;
};

export default Home;
